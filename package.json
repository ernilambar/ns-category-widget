{
  "name": "ns-category-widget",
  "title": "NS Category Widget",
  "version": "3.1.2",
  "license": "GPL-2.0+",
  "main_file": "ns-category-widget.php",
  "repository": "github:ernilambar/ns-category-widget",
  "scripts": {
    "dev": "gulp",
    "deploy": "gulp deploy",
    "pot": "npm run textdomain && npm run language",
    "language": "wpi18n makepot --domain-path=languages --exclude=vendor,deploy,node_modules",
    "textdomain": "wpi18n addtextdomain",
    "install-all": "composer install && npm install",
    "update-all": "composer update && npm update",
    "release": "npm install && composer install --no-dev --no-scripts -o && gulp deploy",
    "wpdeploy": "npm run release && grunt wp_deploy"
  },
  "devDependencies": {
    "browser-sync": "^2.27.4",
    "del": "^5.1.0",
    "dotenv": "^10.0.0",
    "grunt": "^1.4.1",
    "grunt-wp-deploy": "^2.1.2",
    "gulp": "^4.0.2",
    "gulp-load-plugins": "^1.6.0",
    "gulp-zip": "^5.1.0",
    "node-wp-i18n": "^1.2.5"
  }
}
